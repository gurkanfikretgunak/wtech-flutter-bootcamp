


import 'package:flutter/cupertino.dart';



class TopDefPadding extends StatelessWidget {
  const TopDefPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.only(left: 12.0, right: 12.0, top: 10));
  }
}
class BottomPadding extends StatelessWidget {
  const BottomPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.all(10.0));
  }
}
class BottomSearchPadding extends StatelessWidget {
  const BottomSearchPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.only(top: 8.0));
  }
}
class IconPadding extends StatelessWidget {
  const IconPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(left: 30));
  }
}

class TopPadding extends StatelessWidget {
  const TopPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.only(top: 50));
  }
}

class TextFieldPadding extends StatelessWidget {
  const TextFieldPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(left: 10));
  }
}
class SavingSimplePadding extends StatelessWidget {
  const SavingSimplePadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(top: 10));
  }
}

class LeftFifteenPadding extends StatelessWidget {
  const LeftFifteenPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.only(left: 15));
  }
}
class LoyaltyPadding extends StatelessWidget {
  const LoyaltyPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.only(top: 8.0));
  }
}

class DefaultPadding extends StatelessWidget {
  const DefaultPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.all(8));
  }
}

class CardVouchersPadding extends StatelessWidget {
  const CardVouchersPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Padding(padding: EdgeInsets.only(top: 30));
  }
}

class TextFieldBetweenPadding extends StatelessWidget {
  const TextFieldBetweenPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.all(5.0));
  }
}

class LiveTicketBottomPadding extends StatelessWidget {
  const LiveTicketBottomPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(top: 20));
  }
}

class LiveTicketPadding extends StatelessWidget {
  const LiveTicketPadding({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Padding(padding: EdgeInsets.only(top: 40));
  }
}